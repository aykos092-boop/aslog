# 🗺️ Руководство по навигации Swift Ship Connect

## Обзор

Система навигации с автоматическими голосовыми инструкциями, построенная на Mapbox и Web Speech API.

## ✨ Возможности

### 🎯 Основные функции

1. **Автоматическая маршрутизация**
   - Построение оптимального маршрута
   - Альтернативные варианты маршрута
   - Учет пробок в реальном времени

2. **Голосовые инструкции**
   - Автоматическое объявление поворотов
   - Мужской и женский голос
   - Настройка скорости речи (0.5x - 2.0x)
   - Умные подсказки с акцентами на важных маневрах

3. **Карты Mapbox**
   - 6 стилей карт:
     - Светлая
     - Тёмная
     - Улицы
     - Природа
     - Спутник
     - Навигация (с трафиком)

4. **GPS отслеживание**
   - Точное определение местоположения
   - Отображение текущей скорости
   - Компас с ориентацией
   - Режим "Следовать за мной"

5. **Предупреждения**
   - Камеры контроля скорости
   - Превышение скорости
   - Приближение к точке назначения

## 📱 Использование

### Запуск навигации

```typescript
// Из заказа
/navigator/order/:orderId

// Из сделки (для водителя)
/navigator/deal/:dealId

// Прямой маршрут
/navigator?from=Адрес1&to=Адрес2
```

### Голосовые команды

Система автоматически объявляет:

- **За 100м до поворота**: "Через 100 метров, поверните налево"
- **Круговое движение**: "Приготовьтесь! На круговом движении..."
- **Важные повороты**: "Внимание! Поверните направо..."
- **Прибытие**: "Вы прибыли к месту назначения. Приятного дня!"

### Настройки голоса

```typescript
// Переключение голоса
voiceSettings = {
  enabled: true,
  gender: "male" | "female",
  rate: 1.0 // 0.5 - 2.0
}
```

## 🔧 Конфигурация

### Mapbox токен

```typescript
// src/config/mapbox.ts
export const MAPBOX_CONFIG = {
  accessToken: "pk.eyJ1Ijoic3VyZW5hbWVzIiwiYSI6ImNta3UxenZjajF2aDUzY3NhZXNqY3JjeXkifQ.lBzScNO-wcVp0gFnExQx-w",
  // ...
}
```

### Пороги навигации

```typescript
navigation: {
  instructionThreshold: 0.1,  // 100м до объявления
  arrivalThreshold: 0.05,     // 50м до прибытия
  gpsUpdateInterval: 1000,    // Обновление GPS каждую секунду
  followZoom: 17,             // Уровень приближения в режиме следования
}
```

## 🎨 Компоненты

### NavigationInstruction

Отображает текущую инструкцию во время навигации:

```tsx
<NavigationInstruction
  instruction="Поверните направо"
  distance="100 м"
  maneuver="turn-right"
  currentSpeed={60}
  speedLimit={50}
/>
```

### VoiceSettingsPanel

Панель настроек голоса:

```tsx
<VoiceSettingsPanel
  settings={voiceSettings}
  onChange={handleChange}
  isSpeaking={isSpeaking}
  onTestVoice={testVoice}
/>
```

### MapStyleSelector

Выбор стиля карты:

```tsx
<MapStyleSelector
  value={mapStyle}
  onChange={setMapStyle}
/>
```

## 🔊 Голосовые инструкции

### Типы объявлений

1. **Маневры**
   ```typescript
   "Внимание! Поверните налево на улицу Пушкина"
   "Приготовьтесь! На круговом движении выезжайте на третий съезд"
   "Продолжайте движение прямо"
   ```

2. **Расстояния**
   ```typescript
   "Через 500 метров, поверните направо"
   "Через 2 километра, выезд на магистраль"
   ```

3. **Прибытие**
   ```typescript
   "Пункт назначения находится совсем рядом. Менее 100 метров."
   "Вы прибыли к месту назначения. Приятного дня!"
   ```

### Акценты и эмфазис

Система автоматически добавляет акценты на важных маневрах:

- **Повороты**: "Внимание!"
- **Круговое движение**: "Приготовьтесь!"
- **Развилки**: Специальные инструкции

## 📊 API

### useVoiceNavigation

```typescript
const {
  speak,              // Произнести текст
  speakInstruction,   // Произнести инструкцию
  speakProximityAlert,// Объявить близость к цели
  stop,               // Остановить речь
  isSpeaking,         // Идет ли речь
  isPremium,          // Премиум голос
  updateVoiceSettings // Обновить настройки
} = useVoiceNavigation({
  enabled: true,
  language: "ru",
  gender: "male",
  rate: 1.0
});
```

### useSpeedCameraAlerts

```typescript
const {
  nearbyCamera,       // Ближайшая камера
  checkPosition,      // Проверить позицию
  resetAlerts         // Сбросить оповещения
} = useSpeedCameraAlerts(enabled, language);
```

## 🚀 Оптимизация

### Производительность

1. **Дедупликация речи**: Одинаковые фразы не повторяются в течение 15 секунд
2. **Очередь речи**: Максимум 3 фразы в очереди
3. **Кэширование маршрутов**: Офлайн доступ к сохраненным маршрутам
4. **Оптимизация карт**: 512x512 тайлы с правильным zoom offset

### Батарея

- GPS обновляется раз в секунду
- Автоматическое отключение при прибытии
- Эффективное использование Web Speech API

## 🐛 Отладка

### Логирование

```typescript
console.log("Map initialized:", mapStyle);
console.log("Route fetched:", route.distance.text);
console.log("GPS position:", coords);
console.log("Voice instruction:", instruction);
```

### Проблемы и решения

1. **Нет голоса**
   - Проверьте поддержку браузера
   - Убедитесь что голос включен
   - Проверьте разрешения

2. **Карта не загружается**
   - Проверьте Mapbox токен
   - Проверьте интернет соединение
   - Проверьте консоль браузера

3. **GPS неточный**
   - Убедитесь в наличии GPS сигнала
   - Проверьте разрешения локации
   - Используйте высокую точность

## 📝 Примеры

### Полная навигация с голосом

```typescript
const Navigator = () => {
  const [voiceSettings, setVoiceSettings] = useState({
    enabled: true,
    gender: "male",
    rate: 1.0,
  });

  const { speak, speakInstruction } = useVoiceNavigation({
    enabled: voiceSettings.enabled,
    language: "ru",
    gender: voiceSettings.gender,
    rate: voiceSettings.rate,
  });

  const handleNavigation = (step: RouteStep) => {
    speakInstruction(step.instruction, step.distance.text);
  };

  return (
    <UnifiedNavigator
      voiceSettings={voiceSettings}
      onVoiceSettingsChange={setVoiceSettings}
    />
  );
};
```

## 🔐 Безопасность

- Mapbox токен ограничен по домену
- GPS данные шифруются при передаче
- Офлайн кэш защищен
- Разрешения запрашиваются явно

## 📄 Лицензия

© 2024 Swift Ship Connect. Все права защищены.

---

Для получения дополнительной информации посетите [документацию Mapbox](https://docs.mapbox.com/).
