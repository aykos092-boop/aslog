# =====================================================
# AsiaLog - Firebase Configuration
# =====================================================

# Enable Firebase (set to 'true')
VITE_USE_FIREBASE=true

# Firebase Configuration
VITE_FIREBASE_API_KEY=AIzaSyBuht58TZusVJm4do47LSooBWBGSZErsS8
VITE_FIREBASE_AUTH_DOMAIN=asialog-2aa38.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=asialog-2aa38
VITE_FIREBASE_STORAGE_BUCKET=asialog-2aa38.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=472239170057
VITE_FIREBASE_APP_ID=1:472239170057:web:c5267f425f2ab661520ed8
VITE_FIREBASE_MEASUREMENT_ID=G-VZWR0QP89W

# Firebase Emulator (for local development)
VITE_USE_FIREBASE_EMULATOR=false

# Email Service (for OTP codes - optional)
# Можно использовать Firebase Extensions или custom solution
RESEND_API_KEY=re_your_api_key
# или
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password

# Telegram Bot (для Phone OTP)
TELEGRAM_BOT_TOKEN=8361698849:AAFm9dUTOOQpkNIJ-ESopomLB9OLxbKoGAI

# Environment
ENVIRONMENT=production
NODE_ENV=production

# =====================================================
# Setup Instructions:
# =====================================================
#
# 1. Firebase Setup:
#    - Конфигурация уже заполнена выше
#    - Включить Authentication в Firebase Console
#    - Включить провайдеры: Email/Password, Google
#    - Создать Firestore Database
#    - Добавить правила безопасности
#
# 2. Google OAuth (уже настроен в Firebase):
#    - В Firebase Console → Authentication → Sign-in method
#    - Включить Google provider
#    - Добавить authorized domains
#
# 3. Email OTP:
#    - Можно использовать Firebase Extension "Trigger Email"
#    - Или настроить Cloud Function с Resend/SMTP
#
# 4. Firestore Security Rules:
#    rules_version = '2';
#    service cloud.firestore {
#      match /databases/{database}/documents {
#        match /users/{userId} {
#          allow read: if request.auth != null;
#          allow write: if request.auth.uid == userId;
#        }
#        
#        match /email_otp_codes/{docId} {
#          allow create: if true;
#          allow read, update: if request.auth != null;
#        }
#        
#        match /auth_attempts/{docId} {
#          allow create: if true;
#          allow read: if request.auth != null && request.auth.token.admin == true;
#        }
#      }
#    }
#
# =====================================================
