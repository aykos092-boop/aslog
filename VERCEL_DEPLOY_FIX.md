# 🔧 Исправление ошибок деплоя на Vercel

## ❌ Проблема:
Vercel дает ошибку при деплое через репозиторий

## ✅ Решение:

### **1. Обновил vercel.json:**
- Добавил маршрут для API: `/api/(.*)`
- Добавил конфигурацию для функций Supabase
- Исправил роутинг

### **2. Создал .vercelignore:**
- Исключаем Supabase функции (деплоятся отдельно)
- Исключаем временные файлы
- Уменьшаем размер деплоя

### **3. Проверил package.json:**
- Все скрипты на месте
- Правильная структура зависимостей

## 🚀 Как деплоить:

### **Вариант 1: Через Vercel Dashboard**
1. Зайти в [vercel.com](https://vercel.com)
2. Connect Repository → GitHub
3. Выбрать репозиторий `swift-ship-connect`
4. Build Settings:
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

### **Вариант 2: Через Vercel CLI**
```bash
# Установка Vercel CLI
npm i -g vercel

# Деплой
vercel --prod
```

### **Вариант 3: Автоматический деплой**
После обновления файлов в репозитории:
1. Commit и push изменений
2. Vercel автоматически соберет проект
3. Ошибка должна исчезнуть

## 🔍 Что проверять:

### **1. Переменные окружения в Vercel:**
```
VITE_SUPABASE_URL=https://eqrzodfukdnwsogjzmoz.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
VITE_FIREBASE_API_KEY=your_firebase_key
VITE_SITE_URL=https://asloguz.com
```

### **2. Структура проекта:**
```
swift-ship-connect/
├── src/
├── public/
├── package.json
├── vite.config.ts
├── vercel.json
├── .vercelignore
└── supabase/ (игнорируется при деплое)
```

### **3. Логи деплоя:**
Если ошибка остается - проверь логи в Vercel Dashboard:
1. Vercel → Project → Deployments
2. Нажать на последний деплой
3. Посмотреть Build Logs

## 🎯 Результат:
- ✅ **Быстрый деплой** - исключены лишние файлы
- ✅ **Правильный роутинг** - API и статика работают
- ✅ **Автоматический деплой** - при push в репозиторий

**Попробуй снова деплоить через репозиторий - ошибка должна исчезнуть!** 🚀
